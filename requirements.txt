# ============================================================
# requirements.txt — agw-cloud-api
# Vercel Serverless (Python) + desarrollo local
# ============================================================
#  SIN VERSIONES FIJAS a propósito, salvo donde importa.
#
#  Las versiones ancladas de antes (fastapi 0.110, pydantic 2.6) no
#  tienen rueda precompilada para Python 3.13+: pip intenta compilar
#  pydantic-core desde Rust y falla. Eso rompía tanto el arranque local
#  como el build de Vercel, que elige el intérprete por su cuenta.
#
#  Solo se deja lo estrictamente necesario para que la función arranque.
#  Todo lo que sobra en un despliegue serverless alarga el build y sube
#  el tamaño del bundle, que en Vercel tiene tope.
# ============================================================

# ── API ───────────────────────────────────────────────────
fastapi
pydantic
python-multipart

# ── Base de datos ─────────────────────────────────────────
#  psycopg2-binary y no psycopg2: la versión source necesita compilar
#  contra libpq, que no está en el entorno de build de Vercel.
psycopg2-binary

# ── Autenticación ─────────────────────────────────────────
PyJWT
email-validator

# ── Servidor local ────────────────────────────────────────
#  Vercel no lo usa —invoca la app ASGI directamente— pero hace falta
#  para `uvicorn api.index:app` en desarrollo.
uvicorn
