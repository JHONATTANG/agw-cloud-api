# ============================================================
# requirements.txt — agw-cloud-api
# Vercel Serverless (Python 3.11) + Local dev
# ============================================================

# ── API Framework ─────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0

# ── Pydantic v2 ───────────────────────────────────────────
pydantic==2.6.0
pydantic-settings==2.2.0

# ── Database — Synchronous (Vercel Serverless compatible) ─
# psycopg2-binary: Supabase Transaction Pooler (port 6543)
psycopg2-binary==2.9.9

# ── Database — Async (local dev / Docker container) ───────
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.29
alembic==1.13.1

# ── Auth (JWT + password hashing — used by main app) ──────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
PyJWT==2.8.0
email-validator==2.1.1

# ── HTTP client ───────────────────────────────────────────
httpx==0.27.0

# ── Utilities ─────────────────────────────────────────────
python-multipart==0.0.9
structlog==24.1.0

# ── Testing ───────────────────────────────────────────────
pytest==8.1.1
pytest-asyncio==0.23.6
